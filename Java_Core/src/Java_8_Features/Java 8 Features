                    JAVA 8
                       |
       -------------------------------------
       |          |          |             |
    Lambda    Functional   Stream       Optional
              Interface      API
       |
   Method Reference

       -------------------------------------
       |
   Default/Static methods in interfaces

       -------------------------------------
       |
   New Date & Time API

       -------------------------------------
       |
   CompletableFuture


Level 1 — Foundation
Lambda expressions
Functional interfaces
Predicate
Consumer
Supplier
Function
Method references

Level 2 — Most important
Stream API
filter()
map()
sorted()
distinct()
limit()
forEach()
collect()
reduce()
findFirst()
anyMatch(), allMatch(), noneMatch()

Level 3
Optional
orElse()
orElseGet()
orElseThrow()

Level 4
Default methods
Static methods in interfaces
Date/Time API

Level 5
CompletableFuture

------------------------------
Lambda Expressions

A lambda is basically a short way of writing a function/behaviour without creating a separate method or anonymous class.

Syntax:
    (parameters) -> expression

Eg:   (a,b) -> (a+b)   //take a and b and return a+b
      name -> name.toUpperCase() //Take a name and convert it to uppercase

      Multiple statements:

         name -> {
             System.out.println(name);
             return name.toUpperCase();
         }

Imp!!!!!
A lambda does not exist independently.
It needs a Functional Interface.

--------------------------------

Functional Interface

A functional interface is an interface that contains exactly one abstract method

@FunctionalInterface
interface Calculator{
    int calculate(int a,int b);
}

Calculator addition = (a,b) -> (a+b);
System.out.println(addition.calculate(10,20));

---------------------------------------------
Method referencing in Java - ::

MR helps us to use existing methods in a cleaner shorter way

Eg:

1. Without method referencing
list.stream().map(name -> name.toUpperCase());

2. With methods referencing
list.stream().map(String::toUpperCase);

------------------------------------------------

Stream API

A Stream is NOT a data structure.

It doesn't store data.

Instead:

A Stream is a way of processing data.

Suppose:

List<Integer> numbers =
        Arrays.asList(10, 20, 30, 40, 50);

You want numbers greater than 25.

Without Stream:
    for(Integer number : numbers) {
        if(number > 25) {
            System.out.println(number);
        }
    }

With Stream:
    numbers.stream()
           .filter(number -> number > 25)
           .forEach(System.out::println);

Output:
        30
        40
        50

Imagine an office processing employees:

Employees
   ↓
Filter
   ↓
Transform
   ↓
Sort
   ↓
Collect

Java: employees.stream()
               .filter(...)
               .map(...)
               .sorted(...)
               .collect(...);

That's a Stream pipeline.


filter() means:

Keep only elements satisfying a condition.

Example:

List<Integer> numbers =
        Arrays.asList(10, 15, 20, 25, 30);

List<Integer> result =
        numbers.stream()
               .filter(n -> n > 20)
               .collect(Collectors.toList());

Result:

[25, 30]

map() means:

Transform each element into something else.

Example:

List<String> names =
        Arrays.asList("sneha", "rahul", "amit");

Convert to uppercase:

        List<String> result =
                names.stream()
                     .map(name -> name.toUpperCase())
                     .collect(Collectors.toList());

Result:

        [SNEHA, RAHUL, AMIT]