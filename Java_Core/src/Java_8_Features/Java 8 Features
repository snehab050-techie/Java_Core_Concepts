                    JAVA 8
                       |
       -------------------------------------
       |          |          |             |
    Lambda    Functional   Stream       Optional
              Interface      API
       |
   Method Reference

       -------------------------------------
       |
   Default/Static methods in interfaces

       -------------------------------------
       |
   New Date & Time API

       -------------------------------------
       |
   CompletableFuture


Level 1 — Foundation
Lambda expressions
Functional interfaces
Predicate
Consumer
Supplier
Function
Method references

Level 2 — Most important
Stream API
filter()
map()
sorted()
distinct()
limit()
forEach()
collect()
reduce()
findFirst()
anyMatch(), allMatch(), noneMatch()

Level 3
Optional
orElse()
orElseGet()
orElseThrow()

Level 4
Default methods
Static methods in interfaces
Date/Time API

Level 5
CompletableFuture

------------------------------
Lambda Expressions

A lambda is basically a short way of writing a function/behaviour without creating a separate method or anonymous class.

Syntax:
    (parameters) -> expression

Eg:   (a,b) -> (a+b)   //take a and b and return a+b
      name -> name.toUpperCase() //Take a name and convert it to uppercase

      Multiple statements:

         name -> {
             System.out.println(name);
             return name.toUpperCase();
         }

Imp!!!!!
A lambda does not exist independently.
It needs a Functional Interface.

--------------------------------

Functional Interface

A functional interface is an interface that contains exactly one abstract method

@FunctionalInterface
interface Calculator{
    int calculate(int a,int b);
}

Calculator addition = (a,b) -> (a+b);
System.out.println(addition.calculate(10,20));

---------------------------------------------
Method referencing in Java - ::

MR helps us to use existing methods in a cleaner shorter way

Eg:

1. Without method referencing
list.stream().map(name -> name.toUpperCase());

2. With methods referencing
list.stream().map(String::toUpperCase);



